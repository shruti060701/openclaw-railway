[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "node dist/index.js gateway --bind lan --port 18789"
healthcheckPath = "/healthz"
healthcheckTimeout = 120
restartPolicyType = "always"
